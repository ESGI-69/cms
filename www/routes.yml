/:
  controller: main
  action: home

/contact:
  controller: main
  action: contact

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/verify:
  controller: verify
  action: verify

/forget:
  controller: user
  action: pwdforget

/dashboard:
  controller: admin
  action: dashboard
  security: 2
  childrens:
    /pages:
      controller: admin
      action: pages
